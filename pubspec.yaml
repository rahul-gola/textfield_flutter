name: textfield_flutter
description: "Flutter library that provides customizable text input fields with error handling and validation features"
version: 0.0.3
repository: https://github.com/rahul-gola/textfield_flutter.git

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true